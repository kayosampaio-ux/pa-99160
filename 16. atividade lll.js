// atividade

// crie uma função para verificar se um numero
// é positivo ou negativo
const verificar = (num) =>
    // operacao ternária
    num > 0 ? "Positivo" : "Negativo"

const resultado = verificar(10);

console.log(resultado)